<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--  This acts as both parent and aggregator for all sub-modules -->
  <groupId>com.yourorg.classroom</groupId>
  <artifactId>classroom-simulation</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Classroom Automation Simulation (parent)</name>

  <properties>
    <!-- Shared version keys -->
    <java.version>17</java.version>
    <protobuf.version>3.23.4</protobuf.version>
    <grpc.version>2.1.0</grpc.version>
    <protobuf.plugin.version>0.6.1</protobuf.plugin.version>
    <javafx.version>21</javafx.version> 
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <modules>
    <module>proto</module>
    <module>discovery</module>
    <module>services/SmartBoardService</module>
    <module>services/StudentTrackerService</module>
    <module>services/EnvironmentControlService</module>
    <module>central-gui</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <!-- Uses gRPC BOM to unify versions for all gRPC libs -->
      <dependency>
        <groupId>io.grpc</groupId>
        <artifactId>grpc-bom</artifactId>
        <version>${grpc.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Protobuf runtime lib -->
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${protobuf.version}</version>
      </dependency>
      <!-- spring boot -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter-netflix-eureka-server</artifactId>
      </dependency>
      <!-- eureka server -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <pluginManagement>
    <plugins>
      <!-- gRPC & Protobuf generation plugin -->
      <plugin>
        <groupId>org.xolstice.maven.plugins</groupId>
        <artifactId>protobuf-maven-plugin</artifactId>
        <version>${protobuf.plugin.version}</version>
        <configuration>
          <protocArtifact>com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}</protocArtifact>
          <pluginId>grpc-java</pluginId>
          <pluginArtifact>io.grpc:protoc-gen-grpc-java:${grpc.version}:exe:${os.detected.classifier}</pluginArtifact>
          <protoSourceRoot>${project.basedir}/src/main/proto</protoSourceRoot>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>compile</goal>
              <goal>compile-custom</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- JavaFX launcher plugin (runs GUI modules) -->
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>PLACEHOLDER_MAIN_CLASS</mainClass>
          <javafxVersion>${javafx.version}</javafxVersion>
        </configuration>
      </plugin>
    </plugins>
  </pluginManagement>
</project>

